# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# ORM / PostgreSQL (async)
SQLAlchemy>=2.0.30
asyncpg>=0.29.0
greenlet>=3.0.0

# Settings / validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# HTTP client (litellm gateway, YouTube Data API)
httpx>=0.27.0

# 스케줄러 (그룹 순회 폴링/분석 잡)
APScheduler>=3.10.0

# ISO 8601 duration 파싱 (영상 길이)
isodate>=0.6.1

# Secrets encryption
cryptography>=42.0.0

# 세션 쿠키 서명 (Starlette SessionMiddleware)
itsdangerous>=2.1.0

# 테스트
pytest>=8.0.0
pytest-asyncio>=0.23.0
